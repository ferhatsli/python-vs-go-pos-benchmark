# Benchmark-only placeholder values for local/public reproduction.
# Do not reuse these values for any production system.
BENCH_POSTGRES_DB=pos_benchmark
BENCH_POSTGRES_USER=benchmark
BENCH_POSTGRES_PASSWORD=benchmark-local-only
BENCH_POSTGRES_PORT=45432
BENCH_REDIS_PORT=46379
PYTHON_API_PORT=48001
GO_API_PORT=48002
APP_DB_MAX_CONNECTIONS=40
